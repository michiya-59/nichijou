<%= form_with model: @area do |f| %>
  <%= render 'shared/error_messages'%>

  <div>
    <%= f.label :name, "エリア名" %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div>
    <%= link_to '戻る', categories_path, data: {turbo: false}, class: 'btn btn-warning mt-2' %>
    <%= f.submit '更新', data: {turbo: false}, class: 'btn btn-info mt-2' %>  
  </div>
<% end %>

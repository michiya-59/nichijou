<!DOCTYPE html>
<html>
  <head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-9MGT61DTTF"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-9MGT61DTTF');
    </script>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag 'apple-touch-icon.png', rel: 'apple-touch-icon', sizes: '180x180' %>
    <%= favicon_link_tag 'favicon-32x32.png', rel: 'icon', type: 'image/png', sizes: '32x32' %>
    <%= favicon_link_tag 'favicon-16x16.png', rel: 'icon', type: 'image/png', sizes: '16x16' %>
    <%= favicon_link_tag 'site.webmanifest', rel: 'manifest' %>
    <%= favicon_link_tag 'safari-pinned-tab.svg', rel: 'mask-icon', color: '#5bbad5' %>
    <meta name="msapplication-TileColor" content="#ffffff">
    <meta name="theme-color" content="#ffffff">
    <%= show_meta_tags %>
    <%= yield :head %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_importmap_tags %>
    <%= yield(:js) %>

    <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css"/>
  </head>

  <body>
    <% if request.fullpath.include?('admin') %>
      <% if current_admin %>
        <div class="container">
          <% if current_company_admin? %>
            <p class="fz-1_5 fb-mt1"><%= get_store_name %>様<span class="fz-1">ログイン中</span></p>
          <% end %>
          <div class="mt-2 row">
            <!-- サイドバー部分 -->
            <%= render 'shared/admin_header' %>

            <!-- メインコンテンツ部分 -->
            <div class="col-md-9">
              <%= yield %>
            </div>
          </div>
        </div>
      <% else %>
        <div class="container">
          <%= yield %>
        </div>
      <% end %>
    <% else %>
      <%= stylesheet_link_tag "index" %>
      <%= stylesheet_link_tag "destyle" %>

      <%= render 'shared/header' %>
      <%= yield %>
      <%= render 'shared/footer' %>
    <% end %>

  </body>
</html>
